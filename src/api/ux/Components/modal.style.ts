import colorScheme from "../Styles/ColourScheme";

const modalStyle = {
  root: {
    backgroundColor: colorScheme.whiteAlpha,
    paddingTop: 10,
    paddingBottom: 11,
    boxShadow: `2px 2px 4px ${colorScheme.peachBeige}`,
    padding: 20,
    background: "colors.$almond-cream-light-a",
    borderRadius: 4,
    // maxWidth: "90%",
  },
};

export default modalStyle;
